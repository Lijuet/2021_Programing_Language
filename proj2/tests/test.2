procedure foo( inc : integer ) return boolean 
{
        var ret: boolean;

        return ret;
}

procedure Main() return integer
{
	
	var j, k, m, n: integer; 
	var x: string[19];
	var b: boolean;

	j = 2;
	k = - j;
	m = 100 - k * k;
	x[1] = foo( 1 );
	n = printint( m );
	x = "this is a string['5']\n";
	x = "this is \"; x = "invalid";
	x[1] = 'h';
        return x[7+1];
}

